export interface IExperience {
    title: string;
    description: string;
    employer: string;
}

export interface IExperienceFormValues
{
    id: string;
    title: string;
    description: string;
    employer: string;
}